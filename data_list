Data to list:

DNSPACKET:

[source IP/port | dest IP/port]
protocol
query id
question flags
answer flags
counters
RR's
source...

TCP stream

stream length
current seq/ack nums *
window size *

UDP packet

nothing. it's UDP

IP:
packet ID
fragmented?
TTL

MAC headers


